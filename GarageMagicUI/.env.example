# API base URL — only needed if NOT using the Vercel rewrite proxy.
# When deploying to Vercel, /api/* is proxied to Railway via vercel.json.
# Set this if you want the frontend to call the backend directly instead.
# VITE_API_URL=https://garagemagic-production.up.railway.app

